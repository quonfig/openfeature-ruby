# frozen_string_literal: true

source 'https://rubygems.org'

# Use the local sibling SDK during development. Downstream consumers will pull
# from RubyGems via the gemspec runtime dependency.
gem 'quonfig', path: '../sdk-ruby'

group :development do
  gem 'rubocop', '~> 1.86'
end

gemspec
